services:
  - type: web
    name: jarvis-backend
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: ALLOWED_ORIGIN
        sync: false
